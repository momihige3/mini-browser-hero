# プロジェクトルール

## プロジェクト概要

「ミニブラウザヒーロー」は、ビルド工程を持たない静的ブラウザゲームです。

- `index.html`: 画面構造と起動
- `style.css`: レイアウト、レスポンシブ表示、演出
- `game.js`: ゲーム状態、戦闘、装備、描画、音声、デバッグ
- `assets/`: 背景、敵、主人公、カットイン、エフェクト
- ルートのMP3ファイル: BGMとボイス

## 保守ルール

- 機能ごとに正規実装を一つだけ持つ。
- バージョン別の即時実行パッチを追加しない。
- 既存関数を保存してラップする方式は、移行作業以外では使用しない。
- 新しい共通処理は名前付き関数として定義し、呼び出し元を明確にする。
- グローバル状態を追加する場合は`state`へ集約し、保存対象か一時状態かを明示する。
- DOM IDは`index.html`と`game.js`で一対一に対応させる。
- 同じ要素へのイベント登録は一箇所に限定する。
- バージョン更新はパッチ番号を1つずつ上げ、実行時の文字列定義は`index.html`の`data-build-version`一箇所に限定する。
- `game.js`の各処理は共通の`GAME_VERSION`を参照し、過去バージョンの文字列を再代入しない。

## ゲーム動作

- 依頼が保守目的の場合、数値、抽選率、発動間隔、報酬、ゲームバランスを変更しない。
- 天聖騎士、暗黒剣聖、通常ボスの判定はIDまたは共通の敵種別関数で行う。
- 聖剣解放は現行版の単一実装だけを使用する。
- セーブキーと既存保存項目を、移行処理なしに削除・改名しない。

## BGM

- 通常、ボス、暗黒剣聖、天聖騎士、死線の剣舞を単一のBGMコントローラーで管理する。
- 同じ曲に対して`pause()`と`play()`を繰り返さない。
- モード再指定だけで再生位置を先頭へ戻さない。
- BGM切替に`currentTime = 0`を使用しない。
- ミュート時は現在曲を一度停止し、解除時は現在モードの曲を続きから再開する。
- 死線の剣舞終了時は、現在の敵から復帰先BGMを決定する。

## デバッグ機能

- デバッグイベントは起動時に一度だけ登録する。
- `setInterval`によるイベント再登録は禁止する。
- デバッグ操作は通常進行の保存データと区別できる状態にする。
- ボタン名と実際の動作を一致させる。

## 品質ゲート

すべての変更で以下を必須とする。

1. 構文検査成功
2. `git diff --check`成功
3. 対象機能のテスト成功
4. Playwright確認成功
5. コンソールエラーなし
6. 成功後にコミット
7. コミットIDを報告

テスト未実施、Playwright未実施、または失敗が残る状態で完了として扱わない。
