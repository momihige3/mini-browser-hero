# AGENTS.md

この文書は「ミニブラウザヒーロー」を変更するエージェント向けの必須手順です。

## 作業開始前

1. `AGENTS.md`、`PROJECT_RULES.md`、`PLAYWRIGHT_CHECKLIST.md`をすべて読む。
2. `git status --short --branch`と`git diff`を確認する。
3. 既存の未コミット変更を利用者の作業として保護する。
4. 変更対象の定義、呼び出し元、後続の上書きパッチを調査する。

## 実装原則

- 依頼されていない機能追加やゲームバランス変更を行わない。
- セーブデータとの互換性を維持する。
- 同じ関数を後方パッチで再定義せず、正規実装へ統合する。
- イベントは一度だけ登録し、定期的な再登録やDOM複製による解除を行わない。
- BGM、戦闘、状態異常、描画の責務を混在させない。
- バージョン番号は単一の情報源から表示する。
- 機能変更ごとにパッチ番号を1つ上げ、`index.html`の`data-build-version`だけを実行時バージョンの文字列定義とする。
- 変更前後に旧バージョンの実行時代入を検索し、`game.js`や後方パッチへバージョン文字列を直書きしない。
- `package.json`と`package-lock.json`のバージョンは実行時バージョンへ一致させ、Playwrightで一致を確認する。
- 既存の画像・音声ファイル名を変更する場合は、全参照を同時に更新する。

## 必須検証

変更後は、次の順番で検証する。

1. JavaScript構文検査
2. `git diff --check`
3. 変更対象に応じた自動テストまたは隔離テスト
4. `PLAYWRIGHT_CHECKLIST.md`に沿ったPlaywright確認
5. ブラウザコンソールに新規エラーがないことを確認

Playwrightを実行できない、または失敗した場合は作業完了として報告しない。原因と未完了項目を報告する。

## Playwright実行方法

- Codex内蔵ブラウザ接続は使用せず、プロジェクトルートで`npm run test:e2e`を実行する。
- 初回のみ`npm install`で依存関係を導入する。Playwright管理ブラウザのダウンロードは不要で、システムChromeを優先し、Chromeがない場合はEdgeを使用する。
- `npm run test:e2e`が終了コード0で完了し、コンソールエラー、ページ例外、ローカルリクエスト失敗がないことを確認する。

## コミット

- Playwrightを含む必須検証がすべて成功した後にのみコミットする。
- コミットには今回の依頼に関係する変更だけを含める。
- コミット後にコミットIDとテスト結果を報告する。
- `git reset --hard`や利用者の変更を破棄する操作は禁止する。

## 完了報告

完了報告には必ず以下を含める。

- 変更ファイル
- 変更内容
- 動作への影響
- Playwrightで確認した項目
- その他のテスト結果
- コミットID
- 残課題（存在する場合）
